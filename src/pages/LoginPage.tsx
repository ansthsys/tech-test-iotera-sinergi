import LoginForm from "@/components/LoginForm";

export default function LoginPage() {
  return (
    <div className="min-h-[80dvh] flex items-center justify-center p-3">
      <LoginForm />
    </div>
  );
}
